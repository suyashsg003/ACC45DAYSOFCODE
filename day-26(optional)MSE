day-26,on day-26,lol

  heart pattern code copied by net



  #include <iostream>
using namespace std;

int main() {
    int n = 6;
    for (int row = 0; row < n; row++) {
        for (int col = 0; col <= n; col++) {
            if ((row == 0 && col % 3 != 0) || (row == 1 && col % 3 == 0) || (row - col == 2) || (row + col == 8)) {
                cout << "*";
            } else {
                cout << " ";
            }
        }
        cout << "\n";
    }
    return 0;
}
This corrected code will generate the desired "A" pattern with asterisks and spaces.





